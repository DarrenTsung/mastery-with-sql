/*
The first query is getting all films
that are not rated 'G' or 'PG'.

The second query is getting all queries
since if a rating is != 'G' then it must
be != 'PG'.

Therefore the second one is probably a
mistake since it's not filtering any films.
*/
